#define MAPSIZE = 2047
std::vector<std::vector<int>> _matrix( MAPSIZE, std::vector<int>(MAPSIZE, 0) );


// break down into simpler examples
// even if they are incomplete / not the final goal

std::vector<std::vector<int>> _matrix; // an uninitialised vector of vectors

std::vector<int>(MAPSIZE, 0) myTestVariable;  // a 1D vector



